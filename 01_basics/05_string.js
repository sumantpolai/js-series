const name="sumanta"
const repoCount=50

// console.log(name+repoCount+"value");

console.log(`my name is ${name} and my repo count is${repoCount}`);     // morden way or string interpolation


//another way to represent string

const gameName= new String('sumanta')    //benifit is that we can get some extra information like we print each char. or we know about some fun.

console.log(gameName[0]);  // h